{
  "name": "jointmedia",
  "version": "1.0.0",
  "description": "this is a jointmedia automation project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:devServer"      : "npx cypress run --spec cypress/e2e/Tests/MyarTest.cy.js --config-file dev.config.js",
    "test:StaggingServer" : "npx cypress run --spec cypress/e2e/Tests/MyarTest.cy.js --config-file stagging.config.js"
  },
  "author": "Prasenjit Sen",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.11.0"
  }
}
